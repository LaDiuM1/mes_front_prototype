// src/theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        background: {
            // default: "#fafafa", // 전체 배경색
            // paper: "#ffffff", // 카드나 Paper의 배경색
        },
    },
});

export default theme;
